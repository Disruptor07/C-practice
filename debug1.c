#include <stdio.h>
int main() {
    int a = 5;
    int b = 3;
    int sum = a + b;
    int product = a * b;
    
    printf("计算结果: %d + %d = %d, %d * %d = %d\n", 
           a, b, sum, a, b, product);
    
    return 0;
}